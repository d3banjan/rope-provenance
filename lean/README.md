These theorems are extracted from the research monorepo. The file formalizes the role-provenance gate's monotone reachability property — the mathematical claim that ablating more components of a suppressing role gate monotonically raises out-of-role logits — and is cited by the model-internals microsite and the `preregistry/rpcg*` pre-registrations. To build: `lake exe cache get && lake build`. Note: this file is not CI-verified in this repository.
